#include <stdio.h>

int main()
{
    int s = 0;

    while (s <= 60)
    {
        if (s >= 40)
        {

            printf(" My value is %d\n", s);
        }
        s++;
    }

    return 0;
}